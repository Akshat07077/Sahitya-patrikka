# Database
# For local development (direct connection):
# DATABASE_URL="postgresql://username:password@localhost:5432/academic_archive"

# For production/serverless (Supabase - use connection pooling):
# IMPORTANT: Use SESSION mode pooler (not Transaction mode) for Prisma compatibility
# Get the connection string from: Supabase Dashboard > Settings > Database > Connection Pooling > Session mode
# DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"
#
# Transaction mode (port 6543 with transaction pooler) doesn't support prepared statements
# Session mode (port 6543 with session pooler) supports prepared statements and works with Prisma
#
# Alternative: Use direct connection with connection limits (not recommended for high traffic)
# DATABASE_URL="postgresql://user:password@db.project.supabase.co:5432/postgres?connection_limit=1"

# For migrations only (direct connection, not pooled):
# DIRECT_URL="postgresql://user:password@db.project.supabase.co:5432/postgres"

# JWT (if using custom auth)
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"

# Supabase Storage (for file uploads in production)
# Get these from Supabase Dashboard > Settings > API
NEXT_PUBLI_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLI_SUPABASE_ANON_KEY="your-anon-key-here"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Next
NEXT_PUBLIC_APP_URL="http://localhost:3000"

